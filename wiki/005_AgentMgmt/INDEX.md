# Agent Boot Protocol

**此文件是唯一权威 boot 序列。** 按 Step 0-4 顺序执行。

## Step 0 — 术语对齐
读 `active/ctx_main/GLOSSARY.md`。

## Step 1 — 正确性规则
读 `active/ctx_integrity/README.md`，按任务读对应 integrity rules。

## Step 2 — 已定论定义
读 `../002_Architecture/settled.md`。

## Step 3 — 系统现状
读 `active/ctx_main/ctx_system_current.md` + `active/ctx_main/ctx_runtime_architecture.md`。

## Step 4 — 任务路由
查 `active/ctx_main/BOOT_MATRIX.md`，按你的任务类型读取必要文件。
如需全局架构理解，额外读 `../002_Architecture/architecture.md`。

## Historical
`_historical/` 仅供考古，不作为当前实现依据。
